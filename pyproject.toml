[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "opentrons_labware"
version = "0.1.0"
dependencies = [
    "opentrons",
    "pyserial",
    "scikit-optimize",
]

[tool.setuptools.package-dir]
"" = "src"
